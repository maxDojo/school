// All style rules go here in different exported objects ..

const primaryColor = "darkblue";

export const exampleStyle = {
  height: 300,
  backgroundColor: primaryColor,
  // hence we just import {exampleStyle} from 'globalStyles'
  // sorry for being so obvious
};
